// test.js
import { detectAccountNumber } from './src/utils/bankMatcher/index.js';

/**
 * 테스트 시나리오 설정
 * 각 번호는 금융결제원 문서의 과목코드와 자리수를 기반으로 합니다.
 */
const testCases = [
  { 
    acc: "110123456789", 
    desc: "신한은행 12자리 (과목코드 110: 보통예금)" 
  },
  { 
    acc: "00312345601", 
    desc: "기업은행 11자리 (대표코드 003 시작, 과목코드 01)" 
  },
  { 
    acc: "123401567890", 
    desc: "국민은행 12자리 (점번호 4자리 뒤 과목코드 01)" 
  },
  {
    acc: "3011234567890",
    desc: "농협은행 13자리 (과목코드 301: 보통예금)"
  },
  {
    acc: "611123456789",
    desc: "하나은행 12자리 (차세대 과목코드 611: 보통예금)"
  },
  {
    acc: "1002540122738",
    desc: "우리은행 13자리"
  },
  {
    acc: "37040101292010",
    desc: "국민은행 13자리"
  }
];

console.log("==========================================");
console.log("🧪 계좌번호 매칭 정밀 테스트 (Top 3 Ranking)");
console.log("==========================================\n");

testCases.forEach(({ acc, desc }, index) => {
  console.log(`[Test ${index + 1}] ${desc}`);
  console.log(`입력된 번호: ${acc}`);
  
  const results = detectAccountNumber(acc);
  
  if (results.length === 0) {
    console.log("❌ 매칭되는 은행을 찾을 수 없습니다.");
  } else {
    results.forEach((res, rank) => {
      const medal = rank === 0 ? "🥇" : rank === 1 ? "🥈" : "🥉";
      console.log(`${medal} ${rank + 1}위: ${res.bankName.padEnd(6)} | Score: ${res.score.toFixed(1)}`);
    });
  }
  console.log("------------------------------------------\n");
});